import { transitions, layers } from '../common';
// import { Theme } from '../types';
import { colors } from './colors';

export const darkTheme = {
    id: 'dark',
    colors,
    layers,
    transitions,
};
